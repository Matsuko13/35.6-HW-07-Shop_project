<!DOCTYPE HTML>
<html>
<head>
    <title>Shop.Client</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/normalize.css@8.0.1/normalize.css" />
    <link rel="stylesheet" href="/<%= process.env.ADMIN_PATH %>/css/styles.css" />
    <link rel="stylesheet" href="https://fonts.cdnfonts.com/css/plush-trial">
</head>
<body>
    <header class="header">
        <div class="logo"></div>
        <div>
            <% if (user) { %>
                <a href="<%= process.env.ADMIN_PATH %>/new-product>">Add product</a>
            <% } %>    
                <a href= "<%= `/${process.env.ADMIN_PATH}/auth/logout` %>">Logout</a>
        </div>
    </header>
    <div class="container">
        <%- body %>
    </div>
</body>
</html>